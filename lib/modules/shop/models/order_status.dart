/// Order Status Enum
/// 
/// Represents different states of an order that affect UI display.
enum OrderStatus {
  /// Order is active and being processed
  active,
  
  /// Order has been delivered
  delivered,
  
  /// Order is cancelled
  cancelled,
  
  /// Order is being returned
  returning,
}

